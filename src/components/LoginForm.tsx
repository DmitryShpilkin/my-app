import React from 'react';

/**
 * Простая заглушка для компонента формы логина.
 * В реальном проекте здесь будет полноценная форма.
 */
const LoginForm: React.FC = () => {
  return (
    <div>
      <h2>Login Form (Заглушка)</h2>
      <p>Это компонент LoginForm, который мы создали для реэкспорта.</p>
    </div>
  );
};

export default LoginForm;